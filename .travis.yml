language: java
install: true

env:
  global:
    - COVERALLS_REPO_TOKEN=xvogHxLiHie4MgON9hDZ0q9m05RWwcWvW

jdk:
  - oraclejdk8

script:
  - ./gradlew build --scan -s

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

after_success:
- ./gradlew jacocoTestReport coveralls